{
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "test": "vitest run tests/unit",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {},
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/chrome": "^0.0.268",
    "@types/node": "^22.7.4",
    "typescript": "^5.5.4",
    "vite": "^5.4.2",
    "vitest": "^2.0.5"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true,
  "name": "github-repo-visibility",
  "version": "0.1.1",
  "description": "A Manifest V3 Chrome extension that adds repository visibility signals to public GitHub repo pages.",
  "license": "MIT",
  "type": "module"
}
